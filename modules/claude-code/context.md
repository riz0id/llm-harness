For filesystem use `plugin:claude-code-home-manager:filesystem`.
For `git` use `plugin:claude-code-home-manager:git`.
For `gh` use `plugin:claude-code-home-manager:github`.
For `nix` use `plugin:claude-code-home-manager:nixos`.
Do not format code.
Do not lint code.
Do not include testing/verification in plans.
Use `rg` rather than `grep`.
Do not use the `head` command.
Do not use the `tail` command.
End the response when the answer is complete.